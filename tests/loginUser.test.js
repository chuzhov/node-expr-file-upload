const request = require("supertest");

const app = require("../app");

describe("Test API functions", () => {
  test("returns a 200 status code and specific data in the response", async () => {});
});
